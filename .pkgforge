name 'fwknop'
org 'amylum'

licenses 'COPYING'

deps(
  libpcap: '1.8.1-7',
  gpgme: '1.8.0-2',
  'libgpg-error': '1.26-1',
  libassuan: '2.4.3-11'
)

configure_flags(
  prefix: '/usr',
  sbindir: '/usr/bin',
  sysconfdir: '/etc/ssh',
  localstatedir: '/var',
)
remove_linker_archives

cflags
harden

build do
  run 'autoreconf -fiv'
  configure
  make
  install
end

test do
  run 'fwknop -V'
end
