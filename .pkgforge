name 'fwknop'
org 'amylum'

licenses 'COPYING'

deps(
  libpcap: '1.8.1-13',
  gpgme: '1.9.0-1',
  'libgpg-error': '1.27-1',
  libassuan: '2.4.3-12'
)

configure_flags(
  prefix: '/usr',
  sbindir: '/usr/bin',
  sysconfdir: '/etc/ssh',
  localstatedir: '/var',
)
remove_linker_archives

cflags
harden

build do
  run 'autoreconf -fiv'
  configure
  make
  install
end

test do
  run 'fwknop -V'
end
